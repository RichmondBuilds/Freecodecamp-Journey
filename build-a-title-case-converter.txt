** start of script.js **

function titleCase(str) {
  let result = [];
  let splitStr = str.split(" ");
  for (const word of splitStr) {
  let wordCap = word[0].toUpperCase() + word.slice(1).toLowerCase();
  result.push(wordCap);
  }
return result.join(" ");
}

** end of script.js **

