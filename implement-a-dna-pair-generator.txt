** start of script.js **

function pairElement(str) {
 const pairs = {
"A": "T",
"T": "A",
"C": "G",
"G": "C"
 };
return str.toUpperCase().split('').map(base => [base, pairs[base]]);
}


** end of script.js **

