** start of script.js **

function diffArray(arr1, arr2) {
  let filteredArr1 = arr1.filter(item => !arr2.includes(item));
  let filteredArr2 = arr2.filter(item => !arr1.includes(item));
  return filteredArr1.concat(filteredArr2);
}


** end of script.js **

